{
  "name": "flow",
  "version": "0.1.0",
  "scripts": {
    "start": "browser-sync start --server --files index.js index.html",
    "tsc": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Bas Pennings",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^2.13.0",
    "typescript": "1.8.10"
  }
}